# Production environment variables for GitHub Pages build
# These are PUBLIC identifiers (like Firebase config) — safe to include in build
VITE_AWS_REGION=ap-south-1
VITE_COGNITO_USER_POOL_ID=ap-south-1_jp3Rdi0UU
VITE_COGNITO_CLIENT_ID=7tliavfpsko0ugfrkidm8lj2te
VITE_HTTP_API_URL=https://k1f5xsammd.execute-api.ap-south-1.amazonaws.com/

# NOTE: VITE_ADMIN_EMAIL and VITE_ADMIN_PASSWORD are intentionally omitted here.
# The Quick Admin Login button will not appear in the production build.
# Admins must log in manually with their credentials on the Admin tab.
